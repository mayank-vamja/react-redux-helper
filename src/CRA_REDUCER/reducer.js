// import { ActionTypes } from "../actions"

const initialState = {}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    //cases...

    default:
      return { ...state }
  }
}

export default reducer
